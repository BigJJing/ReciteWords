.main{
    position: relative;
}
/* 侧边栏样式 */
.page-slidebar {
    height: 100%;
    width: 65%;
    position: fixed;
    top: 0;
    background-color:white;
    z-index: 0;
}
 /* 主页样式 */
.page-top {
    top: 0;
    width: 100%;
    background-color:white;
    z-index: 0;
    transition: All 0.4s ease;
    -webkit-transition: All 0.4s ease;
}
.page-fix {
    position: fixed;
}
.page-relative {
    position: relative;
}

/* 当屏幕向左滑动，出现侧边栏的时候，主页的动画样式 */
/* scale：取值范围 0~1 ，表示屏幕大小是原来的百分之几，可以自己修改成 0.8 试下*/
/* translate(60%,0%) 表示向左滑动的时候，侧边栏占用平时的宽度为 60%   */
.c-state {
    transform: rotate(0deg) scale(1) translate(65%, 0%);
    -webkit-transform: rotate(0deg) scale(1) translate(65%, 0%);
}

/* 遮盖层样式 */
.cover{
    width: 100%;
    height: 100%;
    background-color:#666;
    opacity: 0.5;
    z-index: 9000;
}

/*
** sliderbar end
*/
.page-slidebar .weui-cell__bd{
  padding-left:10px;
}
.menu-btn{
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background-color: #92b6dc;
    text-align: center;
    line-height: 60px;
    z-index:999;
    position: fixed;
    bottom: 4rem;
    right: 1rem;  
}

.page-slidebar .weui-cell{
    color: #333;
}
.page-slidebar .selected{
    color:#5cabff !important;
}